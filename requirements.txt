# Requirements file for the acis library. 
#
# These dependencies are required to use the library. Dependencies can be
# installed using pip: 
#     pip install -r requirements.txt 

python-dateutil>=1.5
